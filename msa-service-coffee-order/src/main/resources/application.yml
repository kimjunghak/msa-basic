spring:
  h2:
    console:
      path: '/console'

  application:
    name: msa-service-coffee-order

  #Config Server
  config:
    import: "optional:configserver:"

#Eureka Client
eureka:
  client:
    service-url:
      defaultZone: http://localhost:9091/eureka/
